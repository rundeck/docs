# Javascript Node CircleCI 2.1 configuration file
version: 2.1
orbs:
  slack: circleci/slack@6.1.3
jobs:
  build:
    docker:
      - image: cimg/node:24.18.1
        environment:
          NODE_OPTIONS="--max-old-space-size=6144"
    resource_class: medium+

    working_directory: ~/repo

    steps:
      - checkout
      # Download and cache dependencies
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}-{{ checksum "package-lock.json" }}
            # fallback to using the latest cache if no exact match is found
            #- v1-dependencies-
      - run: sudo apt-get update && sudo apt-get install -y apt-transport-https
      - run: sudo apt-get update && sudo apt-get -y install python3-dev python3-pip
      - run: sudo pip3 install --break-system-packages 'awscli==1.29.58'
      - run: npm ci
      - run: npm list
      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}-{{ checksum "package-lock.json" }}
      # run tests!
      - run: node docs/.vuepress/deploy.mjs $CIRCLE_TAG
      - slack/notify:
          channel: C01D3B233DJ
          event: fail
          custom: |
            {
            	"blocks": [
            		{
            			"type": "section",
            			"fields": [
            				{
            					"type": "mrkdwn",
            					"text": ":red_circle: *Job Failed*: ${CIRCLE_JOB}"
            				}
            			]
            		},
            		{
            			"type": "context",
            			"elements": [
            				{
            					"type": "mrkdwn",
            					"text": "*Project*: $CIRCLE_PROJECT_REPONAME"
            				},
            				{
            					"type": "mrkdwn",
            					"text": "*Branch*: $CIRCLE_BRANCH"
            				},
            				{
            					"type": "mrkdwn",
            					"text": "*Author*: $CIRCLE_USERNAME"
            				}
            			]
            		},
            		{
            			"type": "actions",
            			"elements": [
            				{
            					"type": "button",
            					"text": {
            						"type": "plain_text",
            						"text": "View Job"
            					},
            					"url": "${CIRCLE_BUILD_URL}"
            				}
            			]
            		}
            	]
            }
      - slack/notify:
          channel: C01D3B233DJ
          event: pass
          custom: |
            {
            	"blocks": [
            		{
            			"type": "section",
            			"fields": [
            				{
            					"type": "mrkdwn",
            					"text": ":white_check_mark: *Job Success*: ${CIRCLE_JOB}"
            				}
            			]
            		},
            		{
            			"type": "context",
            			"elements": [
            				{
            					"type": "mrkdwn",
            					"text": "*Project*: $CIRCLE_PROJECT_REPONAME"
            				},
            				{
            					"type": "mrkdwn",
            					"text": "*Branch*: $CIRCLE_BRANCH"
            				},
            				{
            					"type": "mrkdwn",
            					"text": "*Author*: $CIRCLE_USERNAME"
            				}
            			]
            		},
            		{
            			"type": "actions",
            			"elements": [
            				{
            					"type": "button",
            					"text": {
            						"type": "plain_text",
            						"text": "View Job"
            					},
            					"url": "${CIRCLE_BUILD_URL}"
            				}
            			]
            		}
            	]
            }
  update_index:
    machine:
      image: ubuntu-2204:current
    resource_class: medium
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: algolia
          command: |
            /bin/bash scripts/_docsearch.sh
workflows:
  publish:
    jobs:
      - build:
          context:
            - slack-secrets
            - Cloudsmith
          filters:
            branches:
              only:
                - /[456]\.[0-9]+\..*/
                - openapi-explorer
            tags:
              only: /^v.*/
      - update_index:
          requires:
            - build
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^v.*/
